language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"
#  - "3.7"
  - "nightly"

install:
  - pip install -r requirements.txt
  - pip install -e .

script:
  - flake8 kattischeck tests
  - pytest -v tests
